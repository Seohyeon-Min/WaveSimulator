# Wave Simulator — 학습 기록

Unity URP에서 바다 파도를 처음부터 직접 구현하고, 이를 실제 플레이 가능한 지형(콜라이더)까지 확장한 과정 기록. 포트폴리오용으로 정리.

## 요약

**기술 스택**: Unity URP, HLSL/ShaderLab, C# 절차적 메쉬 생성, Unity Input System

**직접 내린 핵심 의사결정**
- 여러 개의 Gerstner Wave(sin 기반)를 중첩해 자연스러운 표면을 만들려 했으나, sin 함수 조합이 근본적으로 반복 패턴(격자 무늬)을 벗어날 수 없다는 한계를 스스로 진단하고, **Perlin Noise + FBM 기반 시스템으로 아키텍처를 전환**하기로 결정 ([6단계](#6단계--hash-기반-value-noise--fbm으로-반복-패턴-깨기), [7단계](#7단계--sin-gerstner-완전-제거-노이즈-단독-시스템으로-전환))
- "언덕이 배율로 커졌다 작아지는" 방식은 위치가 고정된 채 크기만 바뀌는 눈속임이라는 걸 파악하고, **시간을 노이즈의 3번째 차원으로 사용하는 3D Perlin Noise**로 재설계해 언덕이 실제로 생성/소멸하도록 개선 ([11단계](#11단계--근본-원인-발견-및-3d-perlin-noise로-재설계))
- 비주얼(GPU 셰이더)과 콜리전(물리 엔진)은 서로 다른 시스템이라는 점을 인지하고, 플레이어가 실제로 타고 점프할 지형은 **C#으로 별도의 메쉬 지오메트리를 생성해 MeshCollider에 연결**하는 구조로 분리 설계 ([14단계](#14단계--실제로-충돌-가능한-반파이프-지형--키-입력으로-솟아오르기꺼지기))
- 손그림/비유 기반 요구사항을 수식으로 번역하는 과정에서 두 차례 형태를 갈아엎었지만, 그때마다 원인을 분석하고 재설계하는 과정을 기록해 시행착오 자체를 학습 자산으로 남김 ([15](#15단계--방향성-있는-파도-단면-시도--천을-손가락으로-밀어올리는-방식으로-단순화)~[17단계](#17단계--방사형-돔--혀-모양-비대칭-스윕-단면으로-재수정))

**문제 해결 하이라이트**: `pow()` 기반 셰이핑이 노이즈의 실제 값 분포와 안 맞아 오히려 평평해지는 문제를 원인 분석 후 대비(Contrast) 보정으로 해결([12단계](#12단계--트러블슈팅-peak-sharpness를-올리면-오히려-평평해지는-문제)), 높이값의 순간적 흔들림에 영향받지 않는 지오메트리 변형을 위해 노이즈의 공간적 경사(gradient)를 유한차분으로 근사해 정점을 실제로 당기는 방식 구현([13단계](#13단계--골은-그대로-두고-봉우리만-뾰족하게-높이가-아닌-수평-당김으로-첨탑-만들기))

## 목표

- GLSL 기초 지식을 바탕으로 Unity의 HLSL(ShaderLab) 커스텀 셰이더 작성법 익히기
- 3D 공간에서 "바다처럼 보이는" 웨이브 노이즈를 만들기 위한 핵심 개념(Gerstner Wave, 노이즈, 버텍스 변위) 학습
- 범위: 이번 학습에서는 **버텍스 변위(vertex displacement)만** 다루고, 노멀 재계산/라이팅/포말 등은 이후 단계로 미룸

## 사전 조사 — 바다 파도를 만들기 위해 필요한 개념

1. **파형의 수학 (버텍스 변위)**
   - 단순 사인파는 정점이 위아래로만 움직여 밋밋하게 보임
   - **Gerstner Wave (Trochoidal Wave)**: 수평 방향으로도 정점을 움직여, 파도 봉우리는 뾰족하고 골은 완만한 실제 파도 형태를 만듦
   - 여러 개의 Gerstner Wave를 다른 방향/파장/진폭/속도로 겹쳐(sum of waves) 자연스러운 표면 생성
   - 파라미터: 방향(direction), 파장(wavelength), 진폭(amplitude), 속도(speed), 스티프니스(steepness)
2. **노이즈 (불규칙성 추가)** — Perlin/Simplex noise, FBM(Fractal Brownian Motion)으로 규칙적인 반복을 깨고 디테일 추가 (이번 학습 범위 밖, 추후 예정)
3. **법선(Normal) 재계산** — 변위 함수의 미분으로 법선을 다시 계산해야 라이팅이 정상적으로 보임 (이번 학습 범위 밖, 추후 예정)
4. **렌더링 방식** — 버텍스 셰이더 방식(이번에 선택) vs Compute Shader + FFT(Tessendorf 방식, 대규모 사실적 바다에 사용)

## 진행 단계

### 1단계 — 씬 준비
- Unity 기본 `Plane` 프리미티브(GameObject > 3D Object > Plane) 사용
- 기본 Plane은 10x10 분할이라 세그먼트가 커서 파도가 각지게(faceted) 보일 수 있음 — 버텍스 사이는 보간되지 않고 변위된 정점만 이동하므로, 파장보다 버텍스 간격이 훨씬 촘촘해야 매끈하게 보인다는 점을 학습
- 우선 기본 Plane으로 시작하고, 결과가 각져 보이면 이후 커스텀 그리드 메쉬(C# 스크립트로 생성)로 교체하기로 결정

### 2단계 — 빈 셰이더로 파이프라인 확인
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- 파도 수식을 넣기 전에, URP 커스텀 셰이더의 기본 구조(Properties / SubShader / Pass / HLSLPROGRAM)부터 검증
- **GLSL ↔ HLSL(URP) 대응 관계 학습**:

  | GLSL | HLSL/URP |
  |---|---|
  | `attribute vec3 position` | `struct Attributes { float4 positionOS : POSITION; }` |
  | `gl_Position = MVP * vec4(position,1)` | `TransformObjectToHClip(IN.positionOS.xyz)` |
  | `gl_FragColor` | `frag()` 함수의 리턴값 (`SV_Target`) |
  | `uniform vec3 uColor` | `float4 _Color;` + Properties 블록 |

- Object Space(`positionOS`) → Homogeneous Clip Space(`positionHCS`) 변환 흐름 이해
- Material을 만들어 `Custom/Ocean` 셰이더 지정 → Plane에 적용 → 단색으로 정상 렌더링되는지 확인 (파이프라인 동작 검증)
- **완료.** Plane에 파란색 단색이 정상적으로 렌더링됨을 확인

#### 트러블슈팅: 머티리얼을 못 넣던 문제
- **증상**: Plane에 Material을 드래그해도 적용이 안 됨
- **원인**: Unity에는 이름이 비슷한 두 가지 리소스가 있음
  - **Physic Material**: 마찰력/탄성 등 물리 속성. `Mesh Collider` 컴포넌트에 넣는 것
  - **Material**: 셰이더/색상 등 렌더링 속성. `Mesh Renderer` 컴포넌트에 넣는 것
  - 렌더링용 Material을 Mesh Collider 슬롯에 넣으려 해서 안 들어갔던 것
- **해결**: `Mesh Renderer` 컴포넌트의 `Materials` 리스트(Element 0)에 드래그
- **추가로 배운 것**: Mesh Renderer의 Materials 배열 크기는 메쉬의 **서브메쉬(submesh) 개수**와 대응됨. 캐릭터 모델처럼 부위별로 다른 재질을 쓰는 메쉬는 서브메쉬가 여러 개라 슬롯도 여러 개가 됨. 기본 Plane은 서브메쉬 1개뿐이라 슬롯도 1개(Size=1)만 있으면 충분

### 3단계 — 사인파 1개로 y축 변위
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- 파도 파라미터를 Properties로 노출: `_Amplitude`, `_Wavelength`, `_Speed`, `_Direction`
- **수식**: `y_offset = amplitude * sin(dot(position.xz, direction) * frequency + time * speed)`, `frequency = 2π / wavelength`
- **핵심 아이디어를 단계별로 분해해서 이해**:
  1. `sin(x)` → 위아래로 진동하는 기본 파형
  2. `amplitude * sin(x)` → 진폭(세로 크기) 조절
  3. `sin(x * frequency)` → 파장(가로 간격) 조절. 원하는 파장이 나오도록 `frequency = 2π / wavelength`로 역산
  4. `sin(x + time * speed)` → 위상이 시간에 따라 바뀌면서 파형이 흐르는 것처럼 보임 (애니메이션)
  5. `x` 대신 `dot(position.xz, direction)` → 1D 파형을 2D 평면의 임의 방향으로 확장. dot product는 "이 위치가 특정 방향으로 얼마나 나아가 있는지"를 스칼라로 뽑아내는 연산이라는 점을 이해 (`direction=(1,0)`이면 사실상 x만, `(0,1)`이면 z만, 대각선이면 x/z를 섞어서 사용)
  6. `direction`을 반드시 `normalize()`해야 하는 이유: 정규화 안 하면 벡터 길이가 dot 결과에 곱해져서 의도치 않게 파장이 늘어나거나 줄어듦
- **GLSL과의 차이점**: HLSL에서 시간은 `_Time.y` (URP Core.hlsl에 내장), 원주율은 `PI` 상수로 이미 정의되어 있어 직접 선언할 필요 없음
- `vert()` 함수에서 `positionOS.y`에 `y_offset`을 더한 뒤 `TransformObjectToHClip()`으로 클립 공간 변환

#### 트러블슈팅: ShaderLab 보일러플레이트 이해
- `Properties` 블록은 HLSL이 아니라 ShaderLab 문법 — 머티리얼 인스펙터에 UI를 자동 생성해주는 역할. GLSL의 `uniform` 선언 + 값 입력 UI가 합쳐진 것
- `SubShader`/`Tags`/`LOD`는 여러 플랫폼·파이프라인에 맞는 셰이더를 엔진이 자동으로 고르게 하는 장치. GLSL엔 대응 개념 없음
- `HLSLPROGRAM ~ ENDHLSL` 안쪽만 실제 HLSL(셰이딩 언어) 코드
- `#pragma vertex vert` / `#pragma fragment frag`: 정점/프래그먼트 셰이더가 한 파일에 같이 있어서, 어떤 함수가 어떤 역할인지 지정해줘야 함 (GLSL은 파일이 분리되어 있고 항상 `main()`이 진입점)
- **Semantic** (`: POSITION`, `: SV_POSITION`) 개념: GLSL의 `layout(location=N)`처럼 정점 속성 위치를 지정하는 게 아니라, "이 필드가 어떤 의미의 데이터인지" 이름표를 붙이는 방식. `SV_POSITION`은 GLSL의 암묵적 내장 변수 `gl_Position`에 대응하는 필수 시스템 값 시맨틱
- `Attributes`(정점 셰이더 입력) / `Varyings`(정점→프래그먼트 출력) 구조체 패턴: GLSL처럼 개별 `in`/`out` 변수를 여러 개 쓰는 대신, 구조체 하나로 묶어서 값을 채우고 `return`하는 방식
- 전역 변수(`float4 _Color;` 등)는 `uniform` 키워드 없이 선언하고, Properties에 적은 이름과 정확히 일치시키면 Unity가 자동으로 값을 바인딩함

### 4단계 — 여러 sin파 겹치기 (중첩, superposition)로 잔잔하게 찰랑거리는 표면 만들기
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **문제 인식**: 파도 1개만 쓰면 모든 지점이 같은 방향 기준으로만 움직여서, 마루/골이 한 방향으로 나란히 줄지어 "규칙적으로 흐르는" 것처럼 보임. 실제 잔잔한 바다처럼 만들려면 특정 방향으로 쏠리는 패턴이 없어야 함
- **원리**: 파동은 선형적으로 중첩된다(더할 수 있다) — 물리학의 중첩 원리. 서로 다른 방향/파장/속도를 가진 sin파 여러 개를 각 정점 위치에서 그냥 더하면, 방향이 다르므로 `dot(position, direction)` 값이 지점마다 제각각으로 나와서 특정 방향으로 흐르는 패턴이 사라지고 국소적으로 울퉁불퉁 찰랑거리는 표면이 됨 (보강/상쇄 간섭)
- **튜닝 포인트**:
  - 진폭(amplitude): 파도마다 작게 → 잔잔하게
  - 파장(wavelength): 다양하게 섞기 (큰 파장=너울, 작은 파장=잔물결)
  - 방향(direction): 여러 각도로 흩뿌려서 한쪽으로 안 쏠리게
  - 속도(speed): 파도마다 다르게 (다 같으면 규칙성이 남음)
- **구현**: 기존 sin파 계산 로직을 `SineWave(positionXZ, direction, wavelength, speed, amplitude, time)` 함수로 추출(재사용 목적) → 메인 파도(인스펙터 Properties로 조절) + 방향/파장/속도가 서로 다른 파도 3개(하드코딩된 상수)를 추가로 호출해서 `yOffset`에 합산
- 아직 개수를 인스펙터에서 조절 가능한 배열로 만들지는 않음 (필요해지면 추후 확장)

### 4-1단계 — 메쉬 촘촘하게 만들기 (커스텀 그리드 메쉬)
- 파일: [`Assets/Scripts/GridMeshGenerator.cs`](Assets/Scripts/GridMeshGenerator.cs)
- **문제**: 기본 Unity `Plane`은 10x10 분할이라 정점 간격이 넓음. 버텍스 변위 방식은 정점 사이가 보간되지 않고 변위된 정점만 이동하므로, 파장(wavelength)보다 정점 간격이 훨씬 촘촘해야 매끈한 곡선처럼 보임 — 그렇지 않으면 각지게(faceted) 보임
- **해결**: C# 스크립트로 `resolution x resolution` 정점의 커스텀 그리드 메쉬를 직접 생성 (표준 그리드 삼각분할: 각 셀을 삼각형 2개로 분할)
- `[ExecuteAlways]` + `OnValidate()`로 Play 모드 없이 에디터에서 즉시 메쉬 갱신되도록 구성
- 이 부분은 파도 학습(셰이더/수식)의 본질이 아닌 지오메트리 생성 로직이라 스크립트를 바로 작성함

### 5단계 — Gerstner Wave로 업그레이드 (뾰족한 봉우리)
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **문제 인식**: 순수 sin파는 봉우리/골이 둥글고 대칭적인 곡선이라, 실제 사진 속 바다의 뾰족한 삼각형 잔물결과 다르게 생김. sin 함수 자체의 한계 — 파라미터를 아무리 조절해도 모양 자체는 안 바뀜
- **원리**: 정점을 y축뿐 아니라 파도 진행 방향(xz)으로도 같이 움직이면(`offset.xz = steepness*amplitude*direction*cos(phase)`), 봉우리 근처에서 정점들이 진행 방향으로 몰려들어 뾰족해지고, 골 근처에서는 퍼져서 완만해짐 → trochoidal(Gerstner) wave의 실제 파도 형태
- `SineWave()` 함수를 `GerstnerWave()`로 확장: `float3` 리턴 (x,y,z 오프셋), `_Steepness`(0~1) 프로퍼티로 뾰족한 정도 조절
- 사진 속 잔잔한 바다에 맞춰 `_Amplitude`/`_Wavelength` 기본값을 더 작게 조정 (완만하고 큰 파도 → 얕고 촘촘한 잔물결)

### 5-1단계 — 임시 조명 (ddx/ddy 노멀 근사)
- **문제**: Unlit 셰이더라 단색으로만 칠해져서 파도의 굴곡이 눈에 안 보임. 정확한 노멀 계산(파도 수식의 미분)은 나중 단계로 미뤄뒀기 때문에, 지금 형태 확인용으로 훨씬 간단한 임시 방법 사용
- **원리**: `ddx()`/`ddy()`는 화면상 옆/아래 픽셀과 비교해서 표면이 얼마나 기울었는지 알려주는 GPU 내장 함수(screen-space derivative). 정점 셰이더에서 넘겨준 월드 좌표(`positionWS`)에 이걸 적용해 `cross(ddy, ddx)`로 대략적인 표면 노멀을 근사
- 근사한 노멀과 빛 방향(`_LightDirection`)을 내적(dot)해서 간단한 램버트 음영(ambient + diffuse) 적용
- 정밀한 방식은 아니지만(픽셀 단위 근사라 약간 각질 수 있음), 코드 몇 줄로 파도 형태를 시각적으로 확인하기엔 충분

### 5-2단계 — 트러블슈팅: 한쪽으로 몰아치는 큰 파도 문제
- **증상**: 인스펙터에서 메인 파도(`_Amplitude`, `_Wavelength`)를 테스트하며 값을 키우다 보니, 나머지 3개의 작은 하드코딩 파도(진폭 0.04~0.06)에 비해 메인 파도 하나가 압도적으로 커져서 그 방향(`_Direction`)으로만 크게 출렁이는 "산맥"처럼 보임
- **원인**: 여러 파도를 중첩할 때, 하나라도 스케일이 크면 그 파도가 전체 형태를 지배해버림 — 잔잔한 바다를 만들려면 합산되는 모든 파도의 스케일이 서로 비슷해야 함
- **해결**: `_Amplitude`(Range 0~0.2), `_Wavelength`(Range 0.5~3) 슬라이더 범위 자체를 제한해서, 인스펙터에서 실수로 지나치게 큰 값을 넣지 못하도록 방지. 기본값도 나머지 3개 파도와 비슷한 스케일(0.08)로 맞춤

### 5-3단계 — 트러블슈팅: 격자 무늬(교차 파도) 문제
- **증상**: 잔잔하게 만들고 나니, 이번엔 표면이 마치 타일/격자처럼 규칙적인 다이아몬드 패턴으로 보임
- **원인**: 파도 개수가 적고(4개), 그중 방향이 거의 직각에 가까운 것들이 있어서 **교차 파도(cross sea)** 패턴이 생김. 게다가 모든 파도가 원점(0,0)에서 위상(phase) 0으로 동시에 시작해서, 원점을 중심으로 한 대칭적인 무늬가 두드러짐
- **해결**:
  1. 파도 개수를 4개 → 6개로 늘리고, 방향을 직각/45도 등 딱 떨어지는 각도가 아닌 어중간한 각도로 재배치
  2. `GerstnerWave()` 함수에 `phaseOffset` 파라미터를 추가해서 파도마다 시작 위상을 서로 다르게 어긋나게 함 (원점 대칭 무늬 방지)
  3. 파장(wavelength) 값들도 서로 간단한 배수 관계가 되지 않도록(예: 1.31, 0.83, 1.62 등 딱 떨어지지 않는 값) 설정해 규칙적인 반복 주기가 덜 생기게 함
- **한계**: 파도(sin) 몇 개를 겹치는 방식만으로는 완전히 무작위한 디테일을 만들기 어려움. 진짜 불규칙한 디테일을 원하면 Perlin/Simplex noise 기반 FBM을 추가하는 게 정석 (추후 확장 가능)

### 6단계 — Hash 기반 Value Noise + FBM으로 반복 패턴 깨기
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **문제 인식**: sin파를 아무리 많이/다양하게 섞어도 근본적으로 "완벽하게 반복(periodic)"되는 함수들의 조합이라, 사람 눈에는 결국 타일/격자처럼 보이는 비트 패턴(beat pattern)이 드러남. 이건 파라미터 튜닝으로 해결되는 문제가 아니라 방법 자체의 한계
- **해결 방향**: Gerstner wave(sin 기반)는 "파도의 큰 형태"만 담당하게 하고, 반복을 깨는 무작위 디테일은 별도의 **노이즈 함수**가 담당하도록 역할 분리
- **구현한 것**:
  1. `Hash21(p)`: 정수 격자점마다 고정된 의사난수 값을 뽑아내는 해시 함수. sin파와 달리 명확한 반복 주기가 없어서 무작위처럼 보임
  2. `ValueNoise(p)`: 격자점들의 해시값을 `smoothstep`으로 부드럽게 보간해서, 뚝뚝 끊기지 않고 완만하게 이어지는 노이즈를 만듦
  3. `FBM(p)` (Fractal Brownian Motion): 같은 노이즈를 스케일(주파수)을 2배씩 키우고 진폭을 절반씩 줄이며 4겹(옥타브) 쌓아서, 큰 굴곡 위에 작은 디테일이 겹쳐진 프랙탈적인 표면을 만듦
  4. `vert()`에서 Gerstner wave 합산 결과(`offset.y`)에 `(FBM(posXZ * noiseScale + time * noiseSpeed) - 0.5) * noiseAmplitude`를 더해서, 파도의 큰 형태는 유지하면서 표면에 무작위 잔물결 디테일을 얹음. 노이즈 좌표에 시간을 더해 디테일이 계속 변하게 함
- **Properties 추가**: `_NoiseScale`(노이즈 촘촘함), `_NoiseAmplitude`(노이즈 세기), `_NoiseSpeed`(노이즈 변화 속도)

### 6-1단계 — 노이즈 종류 비교 및 Perlin Noise로 교체
- **문제**: FBM을 추가했는데도 여전히 격자무늬가 남음. 원인은 sin파의 교차 간섭뿐 아니라, **Value Noise 자체의 알려진 약점**(정사각형 격자 위에서 값(scalar)을 보간하는 방식이라 격자 축 방향으로 은은한 아티팩트가 비침) 때문일 가능성이 큼
- **노이즈 종류 정리**:
  - **Value Noise**: 격자점마다 무작위 "값"을 두고 보간. 구현 간단하지만 격자 티가 남는 단점
  - **Perlin Noise**: 격자점마다 무작위 "방향(그라디언트 벡터)"을 두고, 그 지점까지의 상대 위치와 내적(dot)해서 값을 계산 후 보간. Value Noise의 격자 아티팩트를 해결하려고 Ken Perlin이 고안. 실무 표준
  - **Simplex Noise**: Perlin의 개선판, 정사각형 대신 삼각형/사면체 격자 사용. 아티팩트가 더 적고 고차원에서 효율적이나 구현이 더 복잡
  - **FBM**: 특정 노이즈 종류가 아니라, 노이즈를 여러 스케일로 겹쳐 쌓는 기법. Value/Perlin/Simplex 어디에나 적용 가능
- **해결**: `ValueNoise()`를 `PerlinNoise()`로 교체 — 격자점마다 `RandomGradient()`로 무작위 각도를 뽑고, quintic 보간 곡선(`f*f*f*(f*(f*6-15)+10)`, Perlin이 제안한 개선된 보간 곡선)을 사용
- **진단 실험**: sin(Gerstner) 파도 6개를 잠시 주석 처리하고 노이즈만 남겨서, 격자무늬의 원인이 sin파 교차간섭인지 노이즈 자체인지 분리해서 확인 중. 노이즈만 봤을 때도 격자가 보이면 노이즈 쪽 문제, 안 보이면 sin파 쪽 문제였다는 뜻

### 7단계 — sin(Gerstner) 완전 제거, 노이즈 단독 시스템으로 전환
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **결정**: 격자 무늬 문제와 씨름한 끝에, "잔잔하게 찰랑이는" 목표에는 sin(Gerstner) 기반 파도가 오히려 방해(교차 간섭, 특정 방향 쏠림)가 된다고 판단 — `GerstnerWave()` 함수와 관련 호출을 전부 삭제하고 Perlin Noise FBM만으로 표면을 구성
- **시간 축을 두 가지 역할로 명확히 분리**:
  - **Flow(흐르는 속도)**: `_FlowDirection`, `_FlowSpeed` — 노이즈를 샘플링하는 좌표 자체를 시간에 따라 이동시켜서, 무늬 패턴이 특정 방향으로 흘러가는 것처럼 보이게 함
  - **Pulse(크기가 커졌다 작아졌다 하는 속도)**: `_PulseSpeed` — 무늬의 흐름과 완전히 독립적으로, `0.5 + 0.5*sin(time * pulseSpeed)`로 0~1 사이를 오가는 값을 만들어 진폭 자체에 곱함. "패턴이 어디로 흐르는가"와 "크기가 언제 커지는가"가 서로 다른 시간 함수라서 섞이지 않음
- **최대/최소 높이 직접 지정**: `_MinAmplitude`/`_MaxAmplitude`를 두고, Pulse 값으로 그 사이를 `lerp`. 기존처럼 진폭 하나만 조절하는 것보다 "잔잔할 때 최소 얼마, 셀 때 최대 얼마"를 명확히 지정 가능
- **노이즈 봉우리를 뾰족하게**: Gerstner처럼 정점을 옆으로 밀 수는 없지만(수직 변위만 쓰는 범위라), 노이즈 값 자체를 `pow(abs(centered), peakSharpness)`로 변형해서 비슷한 시각 효과를 냄. 지수(`_PeakSharpness`)가 클수록 중간값들이 0 쪽으로 더 눌려서, 완만한 넓은 언덕 대신 좁고 뾰족한 봉우리 몇 개만 튀어나온 모양이 됨. FBM 결과(대략 0~0.9375 범위)를 0~1로 정규화한 뒤 -1~1로 옮기고 부호(`sign`)를 유지해서 봉우리(+)와 골(-)에 대칭으로 적용
- Properties 정리: sin 관련(`_Amplitude`, `_Wavelength`, `_Speed`, `_Direction`, `_Steepness`) 전부 제거, 노이즈 기반 세트로 재구성

### 8단계 — 펄스를 공간적 노이즈로 바꾸고, 옥타브 수로 매끄러움 조절
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **문제 1**: `pulse = sin(time)`는 표면 전체가 완전히 동시에 커졌다 작아졌다 해서 부자연스러움 — "항상 같은 자리에서 반복"되는 느낌
- **해결**: 펄스도 sin이 아니라 **위치+시간에 따라 달라지는 저주파 노이즈**로 계산 (`FBM(posXZ * _PulseScale + flowDir * _PulseSpeed * t, 2)`). 디테일 노이즈보다 스케일을 훨씬 크게(`_PulseScale`) 잡아서 넓은 영역이 뭉텅이로 같이 움직이되, 노이즈이기 때문에 "지금 커지는 영역"이 위치마다 다르고 시간이 지나면 서서히 다른 곳으로 이동함. 옥타브 2로 고정해서 펄스 자체는 매끄럽게 유지
- **문제 2**: 디테일 노이즈(FBM 4옥타브)를 얹으니 표면이 너무 잘게 울퉁불퉁해서 "각 파도가 산 모양으로 매끄럽게" 보이길 원하는 것과 맞지 않음
- **해결**: `FBM()`에 `octaves` 파라미터를 추가해서 옥타브 수를 조절 가능하게 함(`_Octaves`, 기본값 2). 옥타브를 줄이면 고주파(잘게 쪼개지는) 디테일이 빠지고 저주파(크고 완만한) 성분만 남아서 둥근 산 모양에 가까워짐. 옥타브를 늘리면 더 거칠고 랜덤한 디테일이 추가됨
- `FBM()` 반환값을 사용한 옥타브 수에 맞춰 자체적으로 0~1로 정규화하도록 수정(`value / maxValue`) — 이전엔 4옥타브 고정 가정하에 상수(0.9375)로 나눴는데, 옥타브 수가 가변이 되면서 그때그때 실제 사용된 진폭 합으로 나누도록 변경

### 9단계 — 펄스 대비(Contrast) 추가로 "생겼다 사라졌다"를 뚜렷하게
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **문제**: FBM 노이즈는 값이 평균 0.5 근처에서만 맴돌고 0이나 1 끝까지는 잘 안 감. 그래서 `_MinAmplitude`/`_MaxAmplitude`를 크게 벌려놔도 실제로는 중간값 근처만 오가서 펄스가 "깨작깨작"하게 느껴짐
- **해결**: `_PulseContrast` 추가. `pulse = saturate(0.5 + (pulseRaw - 0.5) * contrast)`로 0.5를 기준으로 값을 밀어내서, 대비가 높을수록 0(잔잔)이나 1(솟음) 양 끝에 값이 몰리게 만듦
- **효과**: 대비를 높이고 `_MinAmplitude`를 0에 가깝게 두면, "완전히 잔잔한 구역"과 "확실히 솟은 구역"이 뚜렷하게 나뉘고, 펄스 노이즈가 위치+시간에 따라 계속 변하므로 한 구역이 잔잔해지면 다른 구역에서 새로 파도가 솟아오르는 것처럼 보임 — 그냥 흐릿하게 커졌다 작아졌다 하는 대신 "여기서 파도가 새로 생성되는" 느낌을 만듦

### 10단계 — 트러블슈팅: "산 하나가 통째로 생겼다 사라지는" 효과가 약함
- **증상**: 펄스 대비를 높였는데도 화면 안에서는 노이즈 가장자리만 살짝 움직이고, 산 하나가 뚜렷하게 부풀었다 꺼지는 게 안 보임
- **원인 분석**:
  1. **스케일 불일치**: `_PulseScale`(펄스 영역의 크기)이 메쉬 전체 크기에 비해 너무 커서, 화면 안에는 사실상 펄스 노이즈의 한 부분(거의 균일한 값)만 보이고 있었음. 산 하나하나가 부풀었다 꺼지려면 펄스 영역이 산 크기와 비슷하거나 더 작아서, 화면에 여러 펄스 영역이 동시에 보여야 함
  2. **흐름과 펄스의 상호작용**: `_FlowSpeed`(무늬가 옆으로 흐르는 속도)가 `_PulseSpeed`(크기 변화 속도)에 비해 너무 빠르면, 산이 "제자리에서 커졌다 작아졌다" 하는 게 아니라 "옆으로 흘러가면서 우연히 사라지는" 것처럼 보여서 원하는 "제자리 호흡" 느낌이 안 남
- **해결**: `_PulseScale` 기본값을 0.3 → 1.0으로 키우고(산 하나하나가 더 작고 여러 개 보이게), `_PulseContrast`를 5로, `_FlowSpeed`는 0.3 → 0.1로 낮춰서(흐름보다 펄스 변화가 더 도드라지게) 제자리에서 볼록/오목해지는 느낌을 강화

### 11단계 — 근본 원인 발견 및 3D Perlin Noise로 재설계
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **근본 원인**: 지금까지의 구조는 사실 `높이 = (고정된 언덕 모양) × (커졌다 작아졌다 하는 배율)`이었음. 언덕의 모양과 위치를 결정하는 노이즈 필드 자체는 한 번도 바뀌지 않고, 곱해지는 배율(펄스)만 시간에 따라 오르내렸던 것 — 그래서 아무리 배율을 조절해도 "같은 자리의 같은 모양"이 커졌다 작아졌다 하는 것 이상이 될 수 없었고, 이게 "고정된 노이즈에서 깔짝거린다"는 느낌의 원인이었음
- **해결 방향**: 지금까지 노이즈는 `Noise(x, z)` 2차원이었는데, 여기에 **시간을 3번째 차원**으로 추가해서 `Noise(x, z, time)`으로 확장. 3D 노이즈 볼륨을 시간 축 방향으로 계속 얇게 썰어서 새로운 단면을 보여주는 것과 같은 원리라, 시간이 지나면 노이즈 구조 자체가 실제로 바뀌어서 언덕이 진짜로 생겼다 사라지고 새로운 곳에서 솟아남 (배율 트릭이 아니라 구조 자체의 변화)
- **구현**:
  1. `Hash31`(3D→스칼라 해시), `RandomGradient3D`(3D 격자점마다 무작위 3D 단위벡터를 구면좌표로 생성)
  2. `PerlinNoise3D`: 정육면체(8개 모서리)의 그라디언트를 계산해서 x→y→z 순서로 3번 보간(trilinear interpolation). 2D 때 4개 모서리를 x→y 2번 보간하던 것의 3D 확장판
  3. `FBM3D`: 기존과 동일하게 옥타브를 쌓되, z(시간)축도 함께 스케일업됨
  4. `vert()`에서 `samplePos = float3(posXZ * noiseScale + flowOffset, time * morphSpeed)`로 노이즈를 샘플링 — xy는 기존처럼 흐름(flow)이 적용되고, z(시간)는 별도의 `_MorphSpeed`로 속도를 독립적으로 조절
- **구조 단순화**: 기존에 있던 "Shape 노이즈 + 별도 Pulse 노이즈 + Pulse Contrast" 3중 구조를 걷어내고, 3D 노이즈 하나로 통합. `_MinHeight`/`_MaxHeight`는 이제 시간에 따라 변하는 배율이 아니라, 노이즈 결과값을 최종적으로 매핑하는 **높이 범위 자체**로 의미가 명확해짐
- **역할 정리**: `_FlowSpeed`(무늬가 옆으로 흐르는 속도, xy축) vs `_MorphSpeed`(언덕이 생겼다 사라지는 속도, 시간/z축) — 서로 완전히 독립적인 두 축을 사용하므로 개념적으로도 코드적으로도 깔끔하게 분리됨

### 12단계 — 트러블슈팅: Peak Sharpness를 올리면 오히려 평평해지는 문제
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **증상**: `_PeakSharpness`를 키우면 봉우리가 뾰족해지긴커녕 표면 전체가 평평해지고 노이즈가 사라진 것처럼 보임
- **원인**: `pow(x, k)`는 `x`가 1(끝)에 아주 가까운 값만 살아남게 하고 나머지는 전부 0 쪽으로 눌러버림 (`pow(0.5,1)=0.5`, `pow(0.5,6)=0.016`). 그런데 FBM 노이즈는 원래 -1~1 끝까지 잘 안 가고 중간(0 근처)에서만 맴도는 성질이 있음 (Pulse 만들 때도 겪었던 것과 같은 근본 원인). 그래서 `_PeakSharpness`를 키우면 노이즈 대부분(끝까지 안 간 중간값들)이 순식간에 0으로 눌려버리고, 진짜 끝값(1에 가까운 값)은 애초에 노이즈 안에 거의 없어서 아무것도 안 남음
- **해결**: `_PeakSharpness`를 적용하기 전에 `_Contrast`로 노이즈 값을 먼저 0~1(또는 -1~1) 끝까지 넓게 펴줌(`centered = clamp(centered * contrast, -1, 1)`). 이렇게 값의 실제 사용 범위를 넓혀놓아야, 그 다음에 오는 `pow()` 기반 sharpening이 "중간을 눌러 진짜 봉우리만 도드라지게 한다"는 원래 의도대로 동작함
- **교훈**: `pow()`나 `saturate()`처럼 "0~1 풀 레인지"를 가정하는 셰이핑 함수를 노이즈에 적용할 때는, 노이즈의 실제 값 분포(대부분 중간에 몰려있음)를 먼저 대비 보정으로 넓혀주는 전처리가 거의 항상 필요함

### 13단계 — 골은 그대로 두고 봉우리만 뾰족하게, 높이가 아닌 수평 당김으로 첨탑 만들기
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **요구사항 1**: 골(trough)은 절대 건드리지 않고 봉우리(양수) 부분만 뾰족하게
  - `GetHeight()` 함수로 높이 계산 로직을 뽑아내고, `centered > 0`(봉우리)일 때만 `_Contrast`/`_PeakSharpness`를 적용. `centered <= 0`(골)이면 원본 노이즈 값을 그대로 사용해서 전혀 변형하지 않음
- **요구사항 2**: 봉우리 끝이 뾰족해지는 게 높이값의 순간적인 흔들림에 영향받지 않아야 함 → 높이(y)만 조작하는 방식 대신, **정점을 수평(xz)으로 실제로 당겨서 모으는** Gerstner 스타일 방식으로 전환
  - **원리**: 노이즈 필드의 **경사(gradient)** — "어느 방향으로 가야 더 높아지는지" — 를 구해서, 그 방향으로 정점을 살짝 당김. 봉우리 주변 정점들이 전부 정상 방향으로 당겨지니까, 실제로 한 지점에 모여들어서 물리적으로 뾰족한 첨탑이 됨 (높이 곡선을 왜곡하는 눈속임이 아니라 진짜 지오메트리 변화)
  - **경사 계산**: 해석적으로 미분하기 어려운 노이즈 함수라서, 상하좌우로 살짝 떨어진 지점의 높이를 각각 구해서 유한差분(finite difference)으로 근사: `gradient = (오른쪽 높이 - 왼쪽 높이, 위쪽 높이 - 아래쪽 높이) / (2 * epsilon)`. 정점 하나당 `GetHeight()`를 5번(자기 자신 + 상하좌우) 호출하게 됨
  - **골에 영향 없음 보장**: `pullFactor = saturate(height / maxHeight) * pullStrength` — height가 0 이하(골)이면 `saturate`가 0이 되어 당김이 전혀 발생하지 않고, 봉우리 꼭대기(height=maxHeight)에 가까울수록 가장 강하게 당겨짐
  - **흔들림에 안정적인 이유**: 경사(공간적 구조)를 따라가는 방식이라, 높이값 하나가 프레임마다 미세하게 출렁여도 "어느 쪽이 오르막인지"라는 방향 자체는 잘 안 바뀌어서 이전보다 안정적임
- Properties 추가: `_PullStrength` (수평으로 당기는 세기)

### 14단계 — 실제로 충돌 가능한 반파이프 지형 + 키 입력으로 솟아오르기/꺼지기
- 파일: [`Assets/Scripts/HalfpipeWaveGenerator.cs`](Assets/Scripts/HalfpipeWaveGenerator.cs), [`Assets/Scripts/WaveInputController.cs`](Assets/Scripts/WaveInputController.cs)
- **배경**: 게임 스코프가 "파도를 찾아서 타고 점프하고 묘기 부리는 게임"으로 커짐. 지금까지의 Ocean.shader는 GPU에서만 정점을 움직이는 비주얼 전용이라 물리 엔진이 모양을 모름 — 실제로 캐릭터가 올라타고 충돌할 지형은 C#에서 진짜 메쉬로 만들고 `MeshCollider`에 연결해야 함
- **반파이프 단면 수식**: `height(z) = amplitude * (1 - cos(z/halfWidth * π)) / 2` — 중앙(z=0)은 0, 양 끝(z=±halfWidth)은 amplitude. cos 곡선이라 중앙은 완만한 바닥, 끝으로 갈수록 가팔라지는 벽이 자연스럽게 나옴. 이 단면을 X축 방향으로 늘려서(grid로 만들어서) 파이프 모양 지형이 됨
- **비주얼과 콜라이더는 완전히 일치하지 않음**: `MeshRenderer`에 Ocean 셰이더 머티리얼을 그대로 씌우면 GPU가 이 위에 추가로 작은 잔물결 노이즈를 얹어서 보여주지만, `MeshCollider`는 C# 쪽의 매끈한 반파이프 형태 그대로 충돌 판정을 함. 잔물결 크기가 작으면 게임플레이상 거의 티가 안 나는, 실무에서 흔한 타협
- **솟아오르기/꺼지기 애니메이션**: `amplitude`를 매 프레임 코루틴으로 0 ↔ `maxAmplitude` 사이에서 `smoothstep`으로 보간하고, 값이 바뀔 때마다 메쉬(+콜라이더)를 다시 생성(`Generate()`). 솟아오르는 건 천천히(`riseDuration`), 꺼지는 건 짧게(`fallDuration`)로 둬서 "훅 꺼지는" 느낌을 만듦
- **키 입력(새 Input System)**: 이 프로젝트의 Active Input Handling이 "Input System Package (New)"로 설정돼 있어서 (`Input.GetKeyDown` 같은 구식 API 대신) `UnityEngine.InputSystem.Keyboard.current.digit1Key.wasPressedThisFrame` 같은 새 API를 사용. `WaveInputController`가 숫자키 1/2/3에 반파이프들을 매핑해서, 하나를 누르면 그 파도만 솟아오르고 나머지는 전부 꺼지게 함(한 번에 하나만 활성화)
- **콜라이더 갱신 트릭**: `MeshCollider.sharedMesh`는 같은 Mesh 객체를 다시 넣어도 변경을 못 알아챌 때가 있어서, `null`로 비웠다가 다시 넣어서 확실히 갱신시킴

**사용법**: 빈 GameObject 2~3개에 `HalfpipeWaveGenerator` 컴포넌트를 추가하고(각각 위치를 다르게 배치) Ocean 머티리얼을 씌운 뒤, 별도의 빈 GameObject에 `WaveInputController`를 추가해서 그 파도 오브젝트들을 `waves` 배열에 순서대로 등록

### 15단계 — 방향성 있는 파도 단면 시도 → "천을 손가락으로 밀어올리는" 방식으로 단순화
- 파일: [`Assets/Scripts/HalfpipeWaveGenerator.cs`](Assets/Scripts/HalfpipeWaveGenerator.cs)
- **1차 시도**: 손그림(비대칭 크레센트 단면 — 뒤쪽은 가파르게 솟고 앞쪽은 정상 근처에서 머물다 급락하는 실제 파도 옆모습)을 보고, `pow()` 두 개를 좌우에 다르게 적용한 비대칭 단면 함수를 구현. `backSharpness<1`(볼록하게 빨리 솟음) / `frontSharpness>1`(정상에서 오래 머물다 급락)로 실제 파도 실루엣을 흉내냄
- **방향 전환**: "천을 바닥에 깔고 한 지점을 손가락으로 아래에서 위로 밀어올리는" 비유를 듣고, 훨씬 단순하고 명확한 모델로 교체함 — 특정 지점을 중심으로 방사형(radial)으로 부드럽게 솟아오르는 돔 형태
- **최종 구현**: `GetHeight(x,z)`가 `pokeOffset`(미는 지점)까지의 유클리드 거리(`Vector2.Distance`)를 구하고, `_radius`로 정규화한 뒤 `cos` 곡선으로 부드러운 돔을 만듦 — 중심(거리 0)에서 최대 높이, 거리가 `radius`면 0, 그 바깥은 완전히 평평. `cos` 곡선을 쓴 이유는 중심과 경계 양쪽에서 기울기가 0(수평)이 되어 "천이 매끄럽게 이어져 감싸는" 느낌을 주기 위함(급격히 꺾이는 부분 없이 자연스럽게 연결됨)
- 기존 길쭉한 "파이프" 형태(`length`+단면) 대신, 정사각형에 가까운 평평한 패치(`patchSizeX`/`patchSizeZ`) 위에 돔 하나가 솟는 구조로 변경. `RiseUp()`/`FallDown()` 애니메이션과 MeshCollider 갱신 로직은 그대로 유지
- **교훈**: 손그림/비유로 설명을 들을 때, 처음 이해한 해석(비대칭 파도 단면)이 실제 의도(방사형 돔)와 다를 수 있음 — 구현하기 전에 비유를 더 구체적으로 확인했으면 시행착오를 줄일 수 있었음

### 16단계 — 뾰족한 돔 → 평평한 정상 + 곡선 벽으로 수정
- 파일: [`Assets/Scripts/HalfpipeWaveGenerator.cs`](Assets/Scripts/HalfpipeWaveGenerator.cs)
- **요구사항**: 정상이 뾰족한 점이 아니라 평평한 평지여야 하고, 그 평지에서 가장자리로 갈 때만 곡선으로 떨어져야 함("ㄴ자 곡선")
- **구현**: `flatTopRatio`(0~0.95) 추가. 중심에서의 정규화 거리 `t`가 `flatTopRatio` 이하면 무조건 `currentAmplitude`(평평한 정상)를 반환. 그 이상이면 `flatTopRatio~1` 구간만 다시 0~1로 재정규화(`wallT`)해서, 그 좁아진 구간 안에서만 기존 cos 곡선으로 떨어지게 함 — 정상 평지의 크기와 벽의 경사 구간을 독립적으로 조절 가능

### 17단계 — 방사형 돔 → "혀 모양" 비대칭 스윕 단면으로 재수정
- 파일: [`Assets/Scripts/HalfpipeWaveGenerator.cs`](Assets/Scripts/HalfpipeWaveGenerator.cs)
- **문제**: 방사형(원형) 돔으로 만들었는데, 실제 원했던 건 "혀 모양" — 한쪽 방향으로는 단면이 계속 이어지고(플레이어가 옆으로 타고 다닐 폭), 진행 방향으로는 비대칭 단면 곡선을 갖는 형태. 손그림으로 정확한 곡률을 확인함
- **곡선 분석 (손그림 기준)**:
  - 뒤쪽(back): 바닥 근처에서는 완만하다가 정상 직전에 거의 수직으로 솟구침 → `pow(t, n)`, `t=0`(바닥)~`1`(정상), `n>1`이면 시작은 느리고 끝에서 가팔라지는 곡선
  - 앞쪽(front, 혀): 정상 바로 옆에서 급격히 떨어지기 시작해서 갈수록 완만해지며 길게 이어짐 → `pow(1-t, n)`, `t=0`(정상)~`1`(바닥), `n>1`이면 시작(정상 옆)이 가파르고 끝(바닥 근처)이 완만해지는 곡선
  - **주의할 점**: 이전 사인/노이즈 작업 때 썼던 "정상에서 오래 머물다 급락"하는 형태(`1-pow(t,n)`)와는 곡률이 반대 방향이라, 같은 `pow()` 트릭이라도 어떤 변수에 적용하느냐(`t` vs `1-t`)에 따라 완전히 다른 모양이 나온다는 걸 다시 확인
- **구조 변경**: 기존 정사각형 패치+방사형 거리 계산을 걷어내고, `rideWidth`(플레이어가 타는 폭 축) × `backWidth`+`frontWidth`(진행 방향 깊이) 그리드로 되돌림 — 이전에 만들었던 "파이프 스윕" 구조와 비슷하지만 곡선 방향이 교정됨
- **교훈**: "혀 모양", "손가락으로 누른 천" 같은 비유는 사람마다 떠올리는 형태가 다를 수 있어서, 실제 구현 전에 간단한 손그림/스케치로 확인하는 게 시행착오를 크게 줄여줌 (이번에도 방사형 돔 → 혀 모양으로 한 번 더 갈아엎음)

### 18단계 — 좌우 가장자리도 수면과 매끄럽게 이어지도록 페이드 추가
- 파일: [`Assets/Scripts/HalfpipeWaveGenerator.cs`](Assets/Scripts/HalfpipeWaveGenerator.cs)
- **문제**: 앞뒤(back/front)로는 바닥까지 곡선으로 내려가지만, 좌우(rideWidth 방향)는 끝까지 같은 높이가 유지돼서, 바다 한가운데 이 파도가 생기면 좌우 끝이 수직으로 뚝 잘린 벽처럼 보임(두부 자른 것 같은 어색한 단면)
- **해결**: `edgeFadeRatio`(0~0.5) 추가. 각 정점의 x좌표(rideWidth 축)가 가장자리에 얼마나 가까운지(`halfWidth - abs(x)`)를 구해서, 가장자리로부터 `rideWidth * edgeFadeRatio`만큼의 폭 안에서 높이를 `smoothstep`으로 0까지 부드럽게 줄임(`y *= edgeFactor`). 그 안쪽은 원래 단면 곡선 그대로 유지
- **효과**: 이제 사방(앞/뒤/좌/우) 모두 가장자리에서 0(수면 높이)으로 수렴해서, 넓은 평면 바다 한가운데 이 오브젝트를 놓아도 이음매 없이 자연스럽게 섞여 보임 — 별도의 옆면/바닥 벽을 만드는 "닫힌 입체" 방식 대신, 경계 높이를 0으로 맞춰서 이음매 자체를 없애는 더 간단한 해법을 선택
- **참고 논의**: 이런 절차적 지형을 Blender 등에서 손으로 모델링하는 방식과 비교했을 때, "파라미터를 바꿔가며 다양한 변형을 자동으로 뽑고 싶다"는 목적에는 지금처럼 코드 기반 절차적 생성이 더 적합하다는 점 확인 (한 번만 쓰는 고정 모양은 보통 Blender로 만들어 FBX로 임포트하는 게 일반적)

### 19단계 — C# 언어 기초 다지기 (동적 할당, 값/참조 타입, 포인터 vs 참조)
- 계기: `HalfpipeWaveGenerator.cs`의 `Generate()`를 직접 처음부터 짜보는 연습을 하다가 `new Vector3[xCount * zCount]` 문법에서 궁금증이 생겨 파고듦
- **동적 할당(`new`)**: 배열 크기(`xCount * zCount`)가 인스펙터 설정값에 따라 런타임에 달라지므로, 컴파일 시점에 크기를 고정할 수 없음. `new`는 프로그램 실행 중에 필요한 만큼의 메모리를 힙(heap)에서 실제로 확보하는 명령이라는 것을 이해
- **값 타입 vs 참조 타입**: `int`/`float`/`Vector3` 같은 값 타입은 스택에 고정 크기로 저장되는 반면, 배열/클래스 같은 참조 타입은 크기가 가변적이라 힙에 할당하고 변수는 그 위치를 가리키는 참조만 가짐
- **포인터 vs 참조**: C#의 참조는 포인터와 비슷하게 메모리 위치를 가리키지만, 주소 연산(`ptr+1` 등)이 불가능하고 직접 해제(`free`)할 필요가 없다는 차이를 정리. 가비지 컬렉터(GC)가 더 이상 참조되지 않는 메모리를 자동으로 회수한다는 개념 학습. C#에도 `unsafe` 블록 안에서 실제 포인터를 쓸 수 있지만, 네이티브 상호작용/극한 최적화가 아니면 실무에서 거의 안 쓴다는 것도 확인
- GLSL 배경만 있던 상태에서 처음 마주친 "일반 프로그래밍 언어의 메모리 모델" 개념이라, 셰이더 언어(모든 게 컴파일 타임에 고정 크기)와의 차이를 비교하며 이해

### 20단계 — 격자 메쉬 생성 직접 코딩 연습: 2D 좌표 → 1D 인덱스 변환

> ### ⭐ 핵심 개념: `int i = zi * xCount + xi;`
>
> **이게 이번 학습에서 가장 중요한 한 줄이었다.** `HalfpipeWaveGenerator.cs`를 처음부터 직접 짜보면서 왜 이중 for문(`zi`, `xi`)을 쓰면서도 배열(`vertices`)은 1차원인지 헷갈렸는데, 답은 **"순회 방식(루프)"과 "저장 방식(배열 차원)"은 서로 완전히 다른 문제**라는 것이었음.
>
> - 격자는 2차원 구조(가로 × 세로)라서, 모든 칸을 빠짐없이 방문하려면 **이중 for문**이 필요함 (바깥 루프가 세로줄을 훑고, 안쪽 루프가 그 줄의 가로를 훑음)
> - 근데 Unity `Mesh.vertices`는 `Vector3[]`, 즉 **1차원 배열**만 받음 (2차원 배열보다 메모리 효율이 좋고 API가 그렇게 설계됨)
> - 그래서 "2차원 좌표(`xi`, `zi`)"를 "1차원 배열 인덱스(`i`)"로 변환해주는 계산이 반드시 필요하고, 그게 바로 `i = zi * xCount + xi`
> - **비유**: 아파트 우편함은 전체를 일렬로 번호 매긴 것(1차원)이지만, 우편배달부는 여전히 "몇 동, 몇 층"(2차원)으로 돌아다니면서 그때그때 "이 동/층은 전체 번호로 몇 번이지?"를 계산해서 우편함을 찾는 것과 같은 원리

- 파일: [`Assets/Scripts/HalfpipeWaveGenerator.cs`](Assets/Scripts/HalfpipeWaveGenerator.cs)
- 곡선 로직(혀 모양 등)을 걷어내고, 격자 메쉬 생성의 기본기(정점 배치, 삼각형 엮기)만 순수하게 처음부터 직접 코딩해보는 연습으로 스크립트를 리셋함
- 진행 중 함께 정리한 개념들: `tx`/`tz`(0~1 보간 매개변수, 관용적으로 `t` 사용), `-1`을 나눗셈에 쓰는 이유(정점 N개 = 간격 N-1개, 마지막 정점이 정확히 비율 1에 도달하게 하기 위함 — "울타리 기둥과 칸" 비유), `i++`/`++i` 차이(for문 증가식에서는 결과가 동일해서 관례적으로 `i++` 사용)

### 21단계 — "높이 함수"의 한계 발견 → 매개변수 경로(parametric path)로 오버행 구현
- 파일: [`Assets/Scripts/HalfpipeWaveGenerator.cs`](Assets/Scripts/HalfpipeWaveGenerator.cs)
- **요구사항**: 가나가와 해변의 파도처럼 위에서 말려 내려오는 형태 — "같은 depth 위치에 정점이 여러 높이로 겹치는" 오버행(overhang)
- **근본 원인**: `GetHeight(depth)`처럼 "위치를 넣으면 높이가 하나 나오는 함수" 구조로는 오버행을 표현할 수 없음 — 함수는 정의상 같은 입력에 출력이 하나뿐이기 때문. 지금까지 만든 모든 파도(sin, Gerstner, 노이즈, 반파이프, 혀 모양)가 전부 이 "높이 함수" 패턴이었고, 그 한계에 처음 부딪힘
- **해결**: 단면을 "함수"가 아니라 "경로(path)"로 재정의. `crossSectionPoints`(제어점 배열, `(depth, heightRatio)` 쌍)를 순서대로 Catmull-Rom 스플라인으로 이어서 곡선을 만듦 — 경로이기 때문에 진행하면서 depth가 줄어들 수도 있고, 그러면 같은 depth를 두 번 지나가면서(한 번은 위로 지나가고, 나중에 아래로 되접혀 지나가면서) 자연스럽게 오버행이 생김
- **핵심 구현**: `SampleCrossSection(t)`가 전역 매개변수 `t`(0~1)를 제어점 배열의 로컬 구간+로컬 매개변수로 변환하고, `CatmullRom(p0,p1,p2,p3,u)`로 그 구간을 보간. `Generate()`에서 `zi`마다 이 경로를 따라 `(depth, height)`를 함께 샘플링 — 기존에는 `depth`를 `tz`로 직접 계산하고 `height`만 별도로 구했는데, 이제 **둘 다 같은 경로에서 나옴**
- **트러블슈팅: 오버행 때문에 옆면이 뚫려 보임**: 기존 "가장자리 페이드"(18단계)는 "한 위치엔 높이가 하나"라는 전제였는데, 오버행에선 그 전제가 깨져서 더 이상 안 통함. 메쉬 자체가 단면을 `rideWidth` 방향으로 얇게 쓸어서(sweep) 만든 "커튼" 형태라, 양 끝에서 보면 뻥 뚫려 보이는 문제 발견
- **해결**: 좌우 양 끝(`xi=0`, `xi=xCount-1`)에 단면 경로를 따라 이미 있는 정점들을 **부채꼴(fan) 삼각분할**해서 뚜껑(cap)을 씌움. 한쪽은 정점 순서대로, 반대쪽은 순서를 뒤집어서 감아야 노멀이 서로 바깥을 향함(안 그러면 한쪽 뚜껑이 안쪽을 향해 뒤집혀 보임)
- **교훈**: 지금까지 파도를 "높이 함수"로만 생각했던 게 암묵적인 가정이었다는 걸, 오버행 요구사항을 만나고서야 깨달음 — 요구사항이 바뀌면 기존 구조의 숨은 전제가 깨질 수 있다는 걸 직접 체감

### 22단계 — 평평한 뚜껑(cap) → 원점으로 오므라드는 테이퍼로 각진 끝 해결
- 파일: [`Assets/Scripts/HalfpipeWaveGenerator.cs`](Assets/Scripts/HalfpipeWaveGenerator.cs)
- **증상**: 21단계에서 좌우 끝을 부채꼴 뚜껑으로 막았더니, 마치 식빵을 칼로 자른 것처럼 평평한 절단면이 각지게 드러남
- **해결**: `edgeTaperRatio` 추가. `rideWidth` 가장자리로 갈수록 단면 좌표(`SampleCrossSection`이 반환한 depth/height 쌍) 전체를 `Vector2.Lerp(Vector2.zero, sample, taperFactor)`로 원점(0,0) 쪽으로 끌어당김 — 18단계 때는 높이(y)만 0으로 줄였는데, 이번엔 오버행이라 단면 좌표 전체(depth, height)를 같이 줄여야 진짜 "한 점으로 둥글게 오므라드는" 모양이 됨
- **효과**: 가장자리에서 단면이 완전히 한 점(원점)으로 수렴하면서, 평평한 뚜껑 대신 카누/럭비공처럼 둥글게 좁아지는 형태가 됨. 뚜껑 삼각형은 그대로 남아있지만 가장자리에서 넓이가 0에 가까워져서 사실상 티가 안 남

### 23단계 — Voronoi Noise + 도메인 워핑으로 스타일라이즈드 표면 무늬(Caustic Network) 추가
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **새로운 노이즈 종류 학습**: Value/Perlin Noise와 원리가 완전히 다른 **Voronoi(Cellular) Noise**. 격자 셀마다 무작위 위치에 "씨앗 점"을 하나씩 두고, 각 픽셀에서 가장 가까운 씨앗(F1)과 두 번째로 가까운 씨앗(F2)까지의 거리를 계산. `F2 - F1`이 0에 가까운 지점 = 두 셀의 경계선이라, 이 값을 이용해 그물망 모양의 선을 뽑아낼 수 있음
- **각진 벌집 패턴 문제와 해결**: Voronoi를 그대로 쓰면 벌집/돌바닥처럼 각진 다각형 패턴이 됨. **도메인 워핑(domain warping)**으로 해결 — Voronoi 결과를 흔드는 게 아니라, Voronoi에 **입력으로 들어가는 좌표 자체**를 노이즈로 미리 흔들어서 넣음. 이미 있던 3D Perlin/FBM 함수를 재사용해서 워프 벡터(`warpX`, `warpY`)를 만들고 시간축(`t * _CausticSpeed`)까지 활용해 애니메이션되게 함
- **UV 대신 월드 좌표 사용**: 메쉬 UV 대신 `positionWS.xz`를 무늬 좌표로 사용 — 오버행/edgeTaper로 메쉬가 찌그러져도 무늬 스케일이 일정하게 유지되고, 나중에 여러 파도 오브젝트가 같은 좌표계를 공유해 무늬가 자연스럽게 이어짐
- **조명과의 결합**: 기존 ddx/ddy 기반 램버트 조명 결과(`litColor`) 위에 흰 망 무늬를 **더하기(additive)**로 얹음 — 포말/하이라이트 표현의 일반적인 방식으로, 기존 조명을 덮어쓰지 않고 그 위에 반짝임만 추가
- **구조적 원칙 재확인**: Vertex Shader(파도 형태, `vert()`)와 Fragment Shader(표면 무늬, `frag()`)를 완전히 분리된 관심사로 다룸 — 이미 처음부터 유지해온 원칙이 새 기능을 붙일 때도 그대로 적용됨
- Properties 추가: `_CausticColor`, `_CausticScale`, `_CausticSpeed`, `_CausticDistortion`, `_CausticLineWidth`, `_CausticIntensity`
- **한계/다음 단계**: 지금은 Voronoi 레이어 1개만 사용. 필요하면 스케일/속도가 다른 레이어를 하나 더 겹쳐서(`max` 또는 덧셈으로 합성) 더 복잡한 빛 갈라짐 효과를 낼 수 있음 (성능 비용과 비교해서 필요할 때 추가하기로 함)

### 24단계 이후 (예정)
- 여러 Gerstner Wave를 겹쳐서 자연스러운 파도 표현
- (추후) 노멀 재계산 + 라이팅
- (추후) FBM 노이즈로 잔물결 디테일 추가

### 24단계 — 진짜 투명도 대신 "색을 겹쳐서" 투명해 보이는 눈속임 + Fresnel
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **문제의식**: "얕은 물 아래 바닥이 비쳐 보이는" 느낌을 원한다고 해서 실제로 Alpha(투명도)를 낮추면, 바다 한가운데서 카메라 각도에 따라 메쉬 뒤의 먼 오브젝트나 스카이박스가 그대로 비쳐서 오히려 이상해짐. 게다가 Unity에서 Transparent 렌더링은 정렬/성능 문제가 따로 있어서 이 프로젝트 단계에는 안 맞음
- **해결**: 실제 투명도(Alpha)는 항상 1(완전 불투명)로 고정하고, 대신 **불투명한 색을 여러 겹 `lerp`로 쌓아서** 투명한 레이어가 얹혀 있는 것처럼 눈속임함
  1. Layer 1 (베이스): 깊은 바다색(`_Color`), 완전 불투명
  2. Layer 2 (표면): 저주파 FBM 노이즈로 밝은 `_SurfaceColor`를 약하게(`_SurfaceBlend`, 기본 25%) 섞음 — "얕은 물이 비쳐 보이는" 느낌
  3. Layer 3 (Caustic): 23단계에서 만든 Voronoi 망 무늬를 `additive`(더하기) 대신 `lerp`(섞기)로 바꿔서, 밝은 흰 선이 자연스럽게 표면 위에 얹힘
  - 세 겹을 다 쌓은 뒤에 램버트 조명(ambient+diffuse)을 한 번에 적용해서, 파도의 굴곡(정점 변위)에 따른 명암이 색 전체에 자연스럽게 반영됨
- **Fresnel 효과 추가**: 카메라 시선 방향(`viewDir`)과 표면 노멀이 이루는 각도로 "보는 각도가 표면에 비스듬할수록 밝아지는" 효과 구현 (`pow(1 - dot(normal, viewDir), power)`). 정면으로 내려다보면 깊은 색이 잘 보이고, 비스듬히 보면 표면이 반짝이는 실제 수면 반사와 비슷한 느낌이 남
- **디자인 원칙**: "겉보기에 투명해 보이는 것"과 "실제로 렌더링 파이프라인에서 투명 처리하는 것"은 다른 문제 — 목적(바다 한가운데서 안정적으로 보이는 것)에 맞게 후자를 의도적으로 피하고 전자만 흉내 내는 실용적 선택
- Properties 추가: `_SurfaceColor`, `_SurfaceBlend`, `_SurfaceNoiseScale`, `_SurfaceNoiseSpeed`, `_FresnelColor`, `_FresnelPower`, `_FresnelIntensity`. `_CausticIntensity`(additive 배율)는 `_CausticBlend`(0~1 섞임 비율)로 의미가 바뀜
- **롤백 후 재적용**: 적용 직후 결과물이 깨져서(정확한 원인은 미확인) 23단계로 한 번 되돌렸다가, 같은 구조를 다시 적용함. 재적용 시 `_CausticIntensity`를 Range(0,3)의 additive 배율 대신 Range(0,1)의 순수 블렌드 비율로 바꾸고 기본값을 0.5로 낮췄고, `_FresnelIntensity` 기본값도 0.5 → 0.3으로 낮춰서, 겹겹이 섞인 색이 과도하게 밝아져 하얗게 날아가는 것을 방지함 (밝기 값들이 서로 누적되는 구조라, 각 레이어의 기본값을 보수적으로 잡는 게 중요하다는 교훈)

### 25단계 — 완성본 백업 + 스테이지별 TODO 뼈대로 재구성
- 파일: [`Assets/Shaders/Ocean_Reference.shader`](Assets/Shaders/Ocean_Reference.shader) (백업), [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader) (연습용)
- **배경**: 노이즈/Voronoi 파이프라인 전체를 다시 설명받은 뒤, "이걸 처음부터 하나하나 TODO로 채워보고 싶다"는 요청. 다만 이전 HalfpipeWaveGenerator 연습 때와 달리, 지금 셰이더는 함수 9개가 서로 강하게 의존하고 있어서 전부 채워야만 결과가 보이는 구조라 "점진적으로 확장"하기 어려움
- **해결**: 완성본(23단계, additive Caustic Voronoi 버전)을 `Custom/Ocean_Reference`라는 별개 이름으로 백업 저장(막히면 비교용, 어떤 머티리얼에도 연결 안 함) → `Ocean.shader`는 모든 핵심 함수를 **컴파일은 되는 임시 상수값**으로 채워서 리셋
- **스테이지 설계** (각 스테이지 끝마다 Unity에서 눈으로 확인 가능하도록 순서 배치):
  1. Hash31 + RandomGradient3D + PerlinNoise3D (셋이 서로 의존해서 묶어서 진행) → 부드러운 언덕 하나 보임
  2. FBM3D (옥타브 누적) → 디테일 추가로 표면이 복잡해짐
  3. GetHeight의 대비+뾰족하게(if 블록) → 봉우리만 뾰족, 골은 그대로
  4. vert()의 경사 당김(이미 완성돼 있음) → 1~3단계가 끝나면 자동으로 첨탑 모양이 살아남
  5. Hash22 + Voronoi2D + frag() 마무리 → 표면에 흰 그물망(Caustic) 무늬 추가
- **설계 원칙**: 각 TODO에 "목표"(왜 필요한지)와 "힌트"(구체적 공식 방향)를 같이 적어서, 이전 HalfpipeWaveGenerator 연습에서 배운 "TODO는 무엇을 하기가 아니라 왜 필요한지부터 서술해야 큰 그림이 보인다"는 교훈을 그대로 적용

### 26단계 — sin/Gerstner 건너뛰고 노이즈부터 처음부터 재구현 + Hash 함수 원리 완전 분해
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **결정**: 25단계에서 만든 5-스테이지 TODO 뼈대가 "한 번에 채워야 할 게 너무 많다"는 피드백을 받고, 실제로 이 프로젝트를 처음 밟았던 순서(빈 셰이더 → 사인파 → ... → 노이즈)로 다시 되짚되, sin/Gerstner 단계는 건너뛰기로 결정 — 최종 코드에 안 남아있는 부분이고, "왜 버렸는지"는 이미 문서와 대화로 충분히 이해했기 때문에 시간을 아끼기로 함
- Ocean.shader를 진짜 2단계(빈 파이프라인 확인용 셰이더)로 리셋한 뒤, 노이즈 관련 Properties/전역변수만 배선하고 `Hash31`부터 다시 직접 작성하는 중
- **Hash 함수의 정체성 학습**:
  - Hash(해시) 함수: "같은 입력 → 항상 같은 출력"이면서 "입력이 조금만 달라도 출력은 완전히 달라 보이는" 결정적 함수. 원래는 해시테이블/체크섬 등에 쓰이지만, GPU에는 진짜 난수 생성기가 없어서 "위치 기반 가짜 난수"를 만드는 용도로 셰이더에서 재활용됨
  - 네이밍 관례 `HashXY` (Shadertoy 등 셰이더 커뮤니티 관습): X=입력 차원, Y=출력 차원. `Hash31`=3D 입력→스칼라 출력, 나중에 만들 `Hash22`=2D 입력→2D 출력
- **`frac(p * 큰상수)`가 난수처럼 보이는 이유**: 큰 상수를 곱하면 입력의 아주 작은 차이가 곱해진 결과에서는 꽤 큰 차이로 벌어지고, `frac()`으로 정수부(원래 입력의 "큰 흐름" 정보)를 버리면 소수부(증폭된 미세한 차이)만 남아서 완전히 다른 값처럼 보임 — 옛날 의사난수 생성기(선형 합동 생성기)와 같은 원리의 "혼돈 증폭" 트릭
- **`dot(p, p+상수)`로 뒤섞는 이유**: 1단계까지만 하면 x,y,z가 서로 완전히 독립적으로 계산되어 축 방향 패턴이 남을 위험이 있음. `dot(p, p+45.32)`를 풀면 `(x²+y²+z²) + 45.32*(x+y+z)`가 되는데, 여기 제곱항(비선형 항)이 들어가는 게 핵심 — 이 스칼라 하나를 x,y,z 전체에 더해서 세 성분이 서로 영향을 주고받게(coupling) 만듦. `dot()`은 이 계산을 한 줄로 압축해주는 내장 함수일 뿐, 수학적으로 반드시 dot이어야 하는 건 아님
  - **더하기 대신 빼도 되는지**: 이 해시는 엄밀하게 유도된 공식이 아니라 경험적으로 잘 섞이면 되는 트릭이라, `+=` 대신 `-=`를 써도 여전히 유효한 해시가 됨 (frac은 음수에도 0~1 값을 돌려주므로 문제없음) — "정답이 하나뿐인 공식"이 아니라는 게 핵심 교훈
- **미분(derivative) 기초 복습**: quintic 보간 곡선의 `S'(0)=0` 의미를 이해하기 위해 미분 개념을 처음부터 다시 정리 — 미분 = 그래프 위 한 점에서의 순간 기울기(접선의 기울기). "위치를 미분하면 속도"라는 물리 비유로 설명. `S'(0)=0`은 "그 지점에서 그래프가 완전히 평평하게 시작한다"는 뜻이고, 격자점 양쪽에서 둘 다 평평하게 시작하니 자연스럽게 이어진다는 논리로 quintic 곡선의 필요성을 재확인
- **업계 실무 참고(코드 vs 노드 그래프)**: 테크아트는 Shader Graph 같은 노드 기반 툴과 HLSL 코드를 상황에 따라 둘 다 씀 — 프로토타이핑/협업엔 노드가 유리하고, 성능 최적화·복잡한 커스텀 로직·엔진 레벨 작업엔 코드가 필수. 시니어로 갈수록 "노드 뒤에서 실제로 무슨 계산이 일어나는지" 코드 레벨로 이해하고 있는 게 차별점이 된다는 점 확인

### 27단계 — `RandomGradient3D` 구현 + 구면좌표계/삼각함수 직관 완전 분해
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- `Hash31` 2회 호출로 경도(`theta`)/위도 높이(`z`) 재료를 만들고, 구면좌표 공식으로 3D 단위벡터를 완성 — 완성함
- **왜 하필 구(sphere)에서 방향을 뽑나**:
  1. 그라디언트는 "방향"만 중요해야 하므로 길이가 항상 정확히 1이어야 함(원점에서 거리가 늘 1인 게 구의 정의라서 자동으로 만족)
  2. `x,y,z`를 각각 독립적으로 무작위로 뽑고 정규화하면 **정육면체 모서리(대각선) 방향이 통계적으로 더 자주 나오는 편향**이 생김(정육면체는 모서리 쪽 부피가 더 넓어서). 구면좌표(위도 높이 `z`를 균등하게 뽑는 방식)는 이 편향 없이 구 표면에 점이 고르게 퍼지는 걸 수학적으로 보장(아르키메데스의 원통 투영 정리)
  - **실무 참고**: 성능이 중요한 셰이더는 이 편향을 감수하고 그냥 단순히 Hash로 x,y,z 뽑아 정규화하는 방법도 실제로 많이 씀 (sin/cos 같은 삼각함수가 GPU에서 비교적 비싼 연산이라). "정답이 하나만 있는 게 아니라 성능-정확도 트레이드오프"라는 걸 다시 확인
- **"편향" 얘기에서 나온 정육면체와, PerlinNoise3D의 격자 정육면체는 서로 다른 개념**이라는 걸 명확히 구분함: 전자는 "x,y,z를 각각 독립적으로 뽑았을 때 그 값들이 채우는 공간의 모양"이고, 후자는 "노이즈 계산을 위해 공간을 나눈 격자 칸" — 이름만 같은 정육면체지 완전히 다른 맥락
- **`sqrt`를 쓰는 이유 재확인**: 구 방정식에서 유도한 `r² = 1-z²`은 반지름의 "제곱"이지 반지름 자체가 아님. 제곱을 되돌리는 연산이 `sqrt`(제곱근)라서, `r = sqrt(1-z²)`로 진짜 반지름 값을 얻어야 함
- **sin/cos을 원 위의 점 좌표로 쓰는 이유(직관 재정립)**: "공식이라 외운다"가 아니라, 애초에 그렇게 정의된 것임을 시계바늘 비유로 재정리. 반지름 1짜리 원 위에서 각도 `theta`만큼 돌아간 바늘 끝과 x축 사이에 생기는 직각삼각형에서, 빗변이 1이라 `cos(theta)=밑변/빗변=밑변 그자체`, `sin(theta)=높이/빗변=높이 그자체`가 됨 → 즉 `(cos theta, sin theta)`가 곧바로 그 점의 좌표. 반지름이 `r`이면 그 좌표에 `r`을 곱하기만 하면 됨(`r*cos`, `r*sin`)
- **STAGE 3 시작 — `PerlinNoise3D`**: 정육면체 8개 모서리에서 `dot(그라디언트, 모서리~p 벡터)`를 구하고, quintic 곡선(`f*f*f*(f*(f*6-15)+10)`)으로 x→y→z 순서 7번 `lerp`해서 하나로 합치는 구조로 TODO 작성. 아직 진행 중

### 27-1단계 — `PerlinNoise3D` 완성: dot 내적의 의미, 트라이리니어 lerp, quintic 계수의 정체
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **`cell`/`f` 재확인**: `cell = floor(p)`는 p가 속한 정육면체의 "가장 작은 좌표" 꼭짓점(기준 꼭짓점), `f = frac(p) = p - cell`은 그 꼭짓점을 원점으로 놓고 봤을 때 p의 **상대 위치**(0~1). 어느 꼭짓점을 기준(0)으로 잡을지는 `floor`가 자동으로 정해준 것뿐이라 "왼쪽아래앞"이라는 이름 자체엔 특별한 의미가 없고, `ceil`을 썼다면 반대쪽 꼭짓점이 기준이 됐어도 알고리즘은 동일하게 작동함
- **8개 dot product(`n000`~`n111`)의 직관**: 각 꼭짓점의 그래디언트(고정된 "바람개비" 방향)와 "그 꼭짓점→p 벡터"를 내적하면, "이 꼭짓점 입장에서 p가 자기가 좋아하는 방향에 있는지"를 스칼라로 뽑아낸 값이 됨(방향이 비슷하면 큰 양수, 반대면 큰 음수, 수직이면 0). 단순히 꼭짓점마다 랜덤 높이값 하나씩 주고 보간하는 Value Noise와 달리, **"거리"뿐 아니라 "방향"까지 반영**되기 때문에 격자 경계가 훨씬 덜 도드라짐 — 이게 Value Noise보다 Perlin Noise가 자연스러워 보이는 이유
- **변수명 규칙**: `n000`의 `n`=noise value, 뒤의 `000`은 그 꼭짓점의 `(x,y,z)` 오프셋을 그대로 이어붙인 것(`cell+(1,0,0)`이면 `n100`) — 8개를 헷갈리지 않고 다음 lerp 단계에서 짝짓기 쉽게 하기 위한 네이밍
- **`lerp`란**: `lerp(a,b,t) = a+(b-a)*t`, "a에서 b까지 가는 길 위에서 t만큼 간 지점의 값"을 구하는 가장 기본적인 선형 보간 함수
- **왜 축 하나씩 나눠서 7번 lerp 하는가(트라이리니어 보간)**: lerp는 한 번에 값 2개만 섞을 수 있는 도구라, 8개를 1개로 합치려면 2개씩 짝지어 줄이는 걸 반복하는 수밖에 없음(8→4→2→1). "N개를 2개씩 묶어 1개로 줄이면 항상 N-1번의 병합이 필요하다"는 일반 규칙(토너먼트 대진표 비유: 8명이 우승자 1명을 가리려면 4+2+1=7경기 필요)이 그대로 적용되어 `4+2+1=7`번의 lerp가 나옴. x→y→z 순서는 관례일 뿐이고 순서를 바꿔도(z→y→x 등) 수학적으로 결과는 동일하며, 아예 8개 항의 가중합(각 항이 `(1-u.x 또는 u.x)*(1-u.y 또는 u.y)*(1-u.z 또는 u.z)`) 한 줄로 풀어써도 같은 값이 나옴 — 7번의 lerp는 "그렇게밖에 안 된다"가 아니라 "읽기 쉽게 나눠 적은 것"
- **quintic 곡선 계수(`6, -15, 10`)가 임의의 숫자가 아님을 직접 검산**: `h(t)=6t⁵-15t⁴+10t³`에 `t=1`을 대입하면 `6-15+10=1`이 정확히 나옴(경계에서 값이 1이 되어야 하는 조건). 이 계수는 "t=1일 때 값=1", "t=1일 때 1차 미분=0", "t=1일 때 2차 미분=0" 3개 조건(t=0쪽은 다항식에 t가 곱해져 있어 자동 만족)을 만족하는 5차 다항식 계수를 구하는 연립방정식의 **유일한 해**라서, 임의로 숫자 하나만 바꾸면(예: `5,-15,10`→`t=1`에서 `0`이 나옴) 경계 조건이 깨져 노이즈가 칸 경계에서 값이 튀는 문제가 생김. `smoothstep`의 `3,-2`도 같은 방식으로 "1차 미분까지만 0"이라는 더 낮은 조건을 풀어서 나온 계수
- **왜 미분(1차/2차) 얘기가 나왔는지, 실제 코드에 미분 계산이 있는지**: 셰이더 코드는 언제나 `h(t)`(함수 값) 자체만 계산하고 미분값을 구하는 코드는 어디에도 없음. "1차/2차 미분이 0"이라는 성질은 Ken Perlin이 계수를 설계할 때 종이 위에서 미리 증명해둔 수학적 보장일 뿐, 런타임에 다시 계산하는 게 아님. 이게 왜 중요하냐면, 최종적으로 화면에 보이는 건 높이(h) 자체가 아니라 "빛이 표면에서 어떻게 튕기는가"(법선, h의 1차 미분)라서, **법선이 부드럽길 원하면 그 재료인 h는 한 단계 더(2차 미분까지) 매끈해야** 함 — 자동차의 위치/속도/가속도 비유(가속도가 뚝 끊기면 위치·속도가 멀쩡해 보여도 "덜컹"함을 느낌)로 정리
- **최종 리매핑 `nxyz*0.5+0.5`**: 7번 lerp를 거친 `nxyz`는 (단위벡터 · 최대 길이 ~0.87 벡터의 내적이라) 대략 -0.7~0.7 범위로 나오는데, 이후 색상/높이 등에 쓰기 편하도록 0~1 범위로 옮기는 표준 트릭(`x*0.5+0.5`: 폭을 절반으로 줄이고 중심을 0.5로 이동)
- **업계 맥락**: smoothstep/smootherstep 같은 이징 곡선, sRGB 감마 근사, Fresnel Schlick 근사처럼, 그래픽 프로그래밍에는 "매번 유도하지 않고 검증된 공식을 그대로 암기해서 갖다 쓰는" 표준 공식들이 많다는 것을 확인. 언리얼 엔진의 머티리얼 에디터에도 Gradient/Perlin/Voronoi Noise가 내장 노드로 존재해서 원리는 동일하지만(격자+그래디언트+dot+부드러운 보간), 그래디언트를 실시간 해시로 뽑는 대신 미리 만든 순열 테이블(permutation table)을 참조하는 구현이 많다는 차이 확인. HLSL 문법 자체는 언리얼의 커스텀 HLSL 노드에서도 거의 그대로 통용됨

### 28단계 — 3D 노이즈(시간=3번째 축) → Flow Noise(그래디언트 회전)로 전환
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- **문제**: `PerlinNoise3D(x, z, 시간)`처럼 시간을 노이즈의 3번째 축으로 넣는 방식(27단계)을 실제로 써보니,
  "봉우리가 완전히 사라지고 다른 랜덤한 위치에서 새로 튀어오르는" 느낌이 원하는 만큼 안 나오고, 봉우리
  위치 자체는 안 바뀐 채 높이만 살짝 흔들리는 것처럼 보였음. `_Contrast`/`_PeakSharpness`(대비를 줘서
  대부분을 평평하게 누르고 극단값만 남기는 방법)로 보완을 시도했지만 근본적인 해결은 아니었음
- **막다른 결론(오답)**: "그럼 격자점에 배정된 그래디언트 값 자체를 시간에 따라 바꾸면 안 되나?"라는
  질문에 처음엔 "그러면 이웃 점들과 안 맞아서 노이즈가 끊긴다"고 판단해 불가능하다고 결론 내림
- **정정**: 이건 틀린 결론이었음 — "값을 다른 값으로 통째로 갈아치우는 것"과 "값을 매끄럽게 변화시키는
  것"은 다른 얘기. 그래디언트가 "각도"라는 걸 이용하면, 그 각도를 시간에 따라 조금씩 **회전**시킬 수
  있고, 회전은 연속적인 변화라 이웃 점들과 안 끊기고 자연스럽게 이어짐. 이게 Ken Perlin과 Fabrice
  Neyret이 2001년에 제안한 **Flow Noise** 기법
- **구현 전환**:
  1. `Hash31`(3D)/`RandomGradient3D`/`PerlinNoise3D`를 전부 삭제하고 `Hash21`(2D)/`RandomGradient2D`/
     `PerlinNoise2D`로 교체 — 시간을 축으로 안 쓰니 공간(x,z) 2축만 있으면 되고, 격자 셀도 정육면체(8
     모서리) 대신 정사각형(4 모서리)으로 단순해짐. `n000~n111`(8개, 7번 lerp) → `n00~n11`(4개, 3번 lerp)
  2. `RandomGradient2D(p, time)`: 격자점마다 `baseAngle`(고정, Hash21로 뽑음) + `rotSpeed`(-1~1, 그 점
     고유의 회전 속도, 역시 Hash21로 뽑되 다른 오프셋 사용) 두 값을 배정. 실제 각도는
     `baseAngle + time*rotSpeed`로 계산해서, 시간이 지나면 바늘이 시계처럼 돌아가게 함. 격자점마다
     회전 속도/방향이 달라야 화면 전체가 통째로 한 방향으로 안 돌고 소용돌이처럼 자연스럽게 보임
  3. `vert()`: `PerlinNoise3D(xz*scale, morphT)` 호출을 `PerlinNoise2D(xz*scale, flowT)`로 교체.
     `_MorphSpeed` 프로퍼티는 그대로 재사용하되 의미가 "시간축 노이즈 속도" → "그래디언트 회전 속도"로 바뀜
- **3D 구면좌표 버전 요약(삭제됐지만 기록으로 남김, 27단계 참고)**: x,y,z를 각각 독립적으로 뽑아 정규화하면
  정육면체 모서리(대각선) 방향이 통계적으로 더 자주 나오는 편향이 생겨서, 위도(z, -1~1 균등)+경도(theta,
  0~2π 균등)로 뽑고 `r=sqrt(1-z²)`(원 반지름)를 곱해 구 표면에 편향 없이 고르게 퍼지는 방향을 만들었음.
  2D는 "원 하나"뿐이라 이런 편향 문제 자체가 없어서, 그냥 각도 하나 뽑아 `cos/sin`만 하면 충분함 — 3D의
  "구 표면 고르게 뽑기" 문제가 2D에선 "원 둘레 고르게 뽑기"로 단순해지고, 각도만 균등하게 뽑으면 원 둘레는
  자동으로 고르게 채워짐
- **교훈**: "이 값은 고정이라 못 바꾼다"는 판단이 항상 맞는 게 아니라, "통째로 갈아치우기"와 "연속적으로
  서서히 변화시키기"를 구분해서 생각해야 함 — 후자는 값의 성질(여기선 "각도"라는 것)을 이용하면 노이즈의
  연속성을 안 깨고도 얼마든지 가능함

### 29단계 — Flow Noise 위에 도메인 워핑/능선 강조/Voronoi 그물망/진짜 노멀/카메라 각도 색까지 쌓아 완성
- 파일: [`Assets/Shaders/Ocean.shader`](Assets/Shaders/Ocean.shader)
- 28단계(Flow Noise 전환)에 이어서, 아래 기능들을 한 번에 하나씩(요청이 오면 그때그때) 쌓아 지금의 완성본을
  만듦. 전부 `Ocean_Reference.shader`(레퍼런스)에 있던 아이디어를 이번 2D Flow Noise 구조에 맞게 재구현한 것.

- **도메인 워핑(Domain Warping)으로 격자 에일리어싱 해결**: `_NoiseScale`을 어느 값(경험적으로 약 1.28) 이상
  올리면 메쉬 정점 간격 대비 노이즈 칸이 너무 작아져서, 노이즈 격자(정사각형)와 메쉬 정점 격자가 서로 어긋난
  주기로 겹치며 규칙적인 무아레(Moiré) 줄무늬가 생기는 에일리어싱 현상을 발견. 격자 "크기" 자체를 칸마다
  다르게 만드는 건 이웃 셀 경계가 안 맞아 불가능하므로, 대신 `GetWarpedPos()`에서 노이즈에 들어가는 좌표
  자체를 더 낮은 주파수(`_WarpScale`)의 노이즈로 미리 살짝 뒤틀어(`_WarpStrength`) 격자 경계를 삐뚤빼뚤하게
  만들어 해결. 격자 경계(항상 정수)라는 전제는 그대로 지켜지니 이어짐 문제도 없음.

- **경사 기반 능선 강조(Pull) + "각지게" 만들기**: 처음엔 "높이가 80% 이상인 봉우리만" 당기는 걸 시도했다가,
  "가파른 곳은 전부"(높이 무관, 경사 크기 기준)로 방향 전환. 유한차분(`hR,hL,hU,hD`)으로 구한 경사
  (`gradient`)의 크기(`slopeMag=length(gradient)`)가 문턱(하드코딩 0)을 넘으면 그 오르막 방향으로 정점을
  수평 이동(`positionOS.xz += gradient*pullFactor`)시켜 능선/절벽처럼 각지게 만듦. **문턱 처리에 smoothstep
  계열이 아니라 순수 선형 `saturate()`만 쓴 게 핵심** — smoothstep은 문턱 근처에서 미분이 0이 되어 "부드럽게
  시작"하는 곡선인데, 여기선 반대로 "문턱에서 뚝 꺾이는" 느낌을 원해서 일부러 미분이 불연속인 1차식을 씀
  (`saturate(x)=clamp(x,0,1)` 자체가 이미 그런 성질을 가짐).

- **경사로 진짜 노멀 계산 (ddx/ddy 폐기)**: `ddx`/`ddy`(화면 픽셀 비교로 노멀을 근사하는 예전 방식)는 삼각형
  하나 전체가 같은 노멀값을 가져서 삼각형 경계가 각지게(faceted) 보이는 문제가 있었음. 이미 `vert()`에서
  구해둔 경사(`gradient`)로 표면의 두 접선(`Tx=(1,dh/dx,0)`, `Tz=(0,dh/dz,1)`)을 외적해서 노멀을 직접 계산
  (`normalOS = normalize(float3(-gradient.x, 1, -gradient.y))`) — 외적은 정의상 두 벡터 모두에 수직인
  벡터를 주므로, 표면에 붙은 두 접선을 외적하면 자동으로 표면 전체에 수직인 노멀이 나옴. 정점마다 다른
  값이라 삼각형 안에서 GPU가 자연스럽게 보간해줘서 각짐이 사라짐. 위치(`TransformObjectToWorld`)와 달리
  방향(노멀)은 비균일 스케일 때문에 별도 변환 함수(`TransformObjectToWorldNormal`)가 필요함.
  - 부호 검산: 오르막(`dh/dx`>0)이면 노멀은 반대쪽(-x)으로 기울어야 표면에 수직 — 실제로 외적 결과가
    `-dh/dx`로 나와서 직관과 일치. 가운데 성분이 항상 정확히 `1`인 이유도 외적 공식(`Tz.z*Tx.x-Tz.x*Tx.z
    = 1*1-0*0 = 1`)으로 손 계산해 확인함.

- **Voronoi 그물망(Caustic) + 마디(node) 강조**: `Hash22`(`Hash21`을 두 오프셋으로 두 번 호출) + `Voronoi2D`
  (3x3 이웃 셀의 씨앗 중 f1=1등/f2=2등 거리, `edge=f2-f1`이 0에 가까우면 경계선)로 기본 그물망을 만듦.
  - **씨앗-to-씨앗 직선(Delaunay)과 셀 경계선(Voronoi)을 혼동**했다가 정정: Voronoi 경계선은 두 씨앗을 잇는
    직선이 아니라 그 직선을 수직으로 가르는 "국경선"이라는 걸 다시 확인함.
    Delaunay(점과 점을 직접 잇기)는 완전히 다른 알고리즘이 필요해서 이번엔 안 감.
  - **벽(edge) vs 마디(node) 구분**: f2뿐 아니라 3등 거리(f3)까지 추적하도록 `Voronoi2D`를 `float2`→`float3`
    로 확장. `corner=f3-f2`가 0에 가까우면(씨앗 3개가 동시에 비슷하게 가까움) 마디, 크면 평범한 벽. 이걸로
    `cornerMask`→`wallStrength`(마디에서 멀면 `_CausticWallBrightness`만, 마디에 가까우면 1.0까지 lerp)를
    만들어 "선일수록 흐리고 마디로 갈수록 진하게" 구현. **한 번에 다 짜지 않고 f3 추가 → corner 계산만
    → 밝기 반영까지 3단계로 나눠서 각 단계 확인받고 진행** — 한 번에 여러 걸 바꾸면 설명을 들어도 이해가
    안 따라온다는 피드백을 받고 이 방식으로 전환함.
  - **2단 도메인 워핑으로 선을 더 구불구불하게**: 저주파(1차, `_CausticDistortion`)로 뒤튼 좌표를
    한 번 더 고주파(2차, `_CausticDistortion2`)로 뒤틂 — 이미 불균일해진 공간 위에 다른 리듬의 흔들림을
    더하면 "큰 굽이 위에 잔물결이 얹힌" 것처럼 훨씬 유기적인 곡선이 나옴 (Inigo Quilez의 반복 도메인 워핑).
  - **지형의 Flow Noise와 동기화**: 그물망만의 독자적인 시간/이동 대신, `vert()`가 지형에 쓰는 것과 똑같은
    `flowOffset`(`_FlowDirection`/`_FlowSpeed`)과 `flowT`(`_MorphSpeed`)를 그대로 재사용해서 그물망이
    지형과 같은 리듬으로 같이 흘러가게 함. `_CausticSpeed`는 절대 속도가 아니라 지형 변형 속도 대비 배율로
    의미가 바뀜.

- **카메라 각도/거리 기반 색 레이어 2개 추가**:
  - **Scatter(깊이 산란)**: 카메라 시선이 노멀과 가까울수록(정면으로 내려다봄) 어두운 물속 색(`_ScatterColor`)
    쪽으로, 비스듬할수록 원래 표면색이 보이게 `lerp`. 실제 물의 프레넬 반사율이 정면에서 낮고(반사 적음
    → 물속이 비침) 비스듬할수록 높아지는(반사 강함 → 표면색 지배) 원리를 그대로 이용. `GetCameraPositionWS()`
    (URP 공식 함수, 옛날 Built-in RP 문법 `_WorldSpaceCameraPosition`을 그대로 쓰면 컴파일 에러 남)로
    카메라 위치를 구해 `viewDir`을 만들고, `dot(normalWS, viewDir)`로 각도를 판단.
  - **Whitecap(빛 산란)**: Scatter랑 정반대 조건 - "비스듬하고(`1-viewDotNormal`) + 카메라에서 멀수록"
    하얗게 빛나 보이게(`_WhitecapColor`) `lerp`. 두 조건을 곱해서(AND) 비스듬해도 가까우면 약하고, 멀어도
    정면이면 약하게 만듦 — 멀리서 본 실제 바다 수평선 근처가 하얗게 뭉개져 보이는 느낌을 흉내냄.

- **작업 방식 관련 피드백**: 큰 기능(Voronoi 마디 강조, 이번 단계 전체)을 한 번에 다 구현하고 설명하면
  이해가 안 따라온다는 피드백을 여러 번 받음 → 이후로는 "코드 한두 줄만 바꾸고 확인받은 뒤 다음으로" 방식으로
  전환. 또한 코드를 바꾸기 전에 "왜 이걸 해야 하는지" 목적부터 다시 설명하고 들어가는 게 필요했음(예:
  Voronoi 씨앗-거미줄 오해, `f1`/`f2`가 뭔지 재확인 등) — 새 개념을 도입할 때마다 "그래서 이게 왜 필요한데?"
  질문이 나오면 목적부터 다시 짚어주는 게 효과적이었음.

## 참고

- Unity URP 셰이더그래프 패키지 샘플에 `GerstnerWave.shadergraph` 참고 자료 존재 (`Samples~/FeatureExamples/VertexAnimation/GerstnerWave`)
